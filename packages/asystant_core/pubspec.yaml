name: asystant_core
description: Typed local tools, session contracts and gateway transport for embedded assistants.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: '>=3.13.0 <4.0.0'
dependencies:
  collection: ^1.19.1
  http: ^1.6.0
  result_controller: ^1.3.0
dev_dependencies:
  test: ^1.29.0
